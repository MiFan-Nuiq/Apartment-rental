# 自动化测试工程依赖清单 —— 在 test/automation 目录下执行 pip install -r requirements.txt 安装
# 说明：各依赖用途见行内注释；版本为推荐版本，可结合本地 Python 版本调整

requests>=2.31.0        # HTTP 请求库：接口层与请求工具（utils/request_util.py）的基础
pytest>=8.0.0           # 测试框架：用例收集、执行、fixture 与 marker 管理
allure-pytest>=2.13.2   # Allure 报告插件：生成 allure-results 报告数据
pytest-xdist>=3.5.0     # 并行执行插件：pytest -n auto 多进程加速用例（可选）
pymysql>=1.1.0          # MySQL 驱动：utils/db_util.py 直连数据库做一致性校验
pyyaml>=6.0.1           # YAML 解析：读取 data/*.yaml 数据驱动用例集
playwright>=1.40.0      # 浏览器自动化库：UI 用例（pages/ + testcases/test_ui_login.py）依赖
pytest-playwright>=0.4.0  # pytest 与 Playwright 集成插件
# 说明：本项目 UI 层使用 Playwright（原 ui_test_login.py 即为 Playwright 实现），因此不引入 selenium。